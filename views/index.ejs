<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>screenTime</title>
  <link rel="stylesheet" type="text/css" href="styles/main.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
    rel="stylesheet">
  <script src="https://kit.fontawesome.com/a613df3a16.js" crossorigin="anonymous"></script>
</head>

<body>
  <div class="page">
    <h1><span class="screen">screen</span><span class="time">time</span></h1>

    <% if(page !=='RESULTS' ) { %>
      <div class="main-img"></div>
      <% } %>

        <% if(page==='HOME' ) { %>
          <div class="ftr-card">
            <div class="home-card-title">
              <span>Welcome to </span>
              <span class="screentime">
                <span>screen</span>
                <span class="pink-text">time</span>
              </span>
            </div>

            <p class="home-card-content">
              <span> Keep harmful rays at bay with our location-based</span>
              <span class="pink-text" style="font-weight: 700;">UV detection</span>.
              <span> Staying safe is just a tap away!</span>
            </p>

            <form id="home" method="post">
              <button id="start" type="submit" formaction="/location" class="btn">Get started</button>
            </form>
          </div>
          <% } %>
            <% if(page==='LOCATION' ) { %>
              <div class="ftr-card">
                <h2>
                  <i class="fa-solid fa-location-dot pink-text"></i>
                  <span>Add a location</span>
                </h2>

                <form id="location" method="post">
                  <input type="text" id="location" name="location" />

                  <% if(data?.error ) { %>
                    <p class="err-msg">
                      <i class="fa-solid fa-triangle-exclamation"></i>
                      <span class="poppins-semibold">Warning: </span>
                      <span>
                        <%= data.error %>
                      </span>
                    </p>
                    <% } %>
                      <button type="submit" value="RESULTS" formaction="/results">Submit</button>
                </form>
              </div>
              <% } %>
                <% if(page==='RESULTS' ) { %>
                  <div class="results">
                    <span class="uv-index">
                      <%= data.uvIndex %>
                    </span>
                    <div>
                      <span class="category">
                        <%= data.uvIndexCategory%> Exposure
                      </span>

                      <p class="message">
                        <%= data.message %>
                      </p>
                      <p class="location">
                        <%= data.location %>
                      </p>
                    </div>


                    <form id="results" method="post">
                      <button type="submit" value="RESULTS" formaction="/new-search">Search again</button>
                    </form>
                    <% } %>
                  </div>

  </div>
</body>

</html>